spring.application.name=Student Management System

#database config
spring.datasource.url= jdbc:mysql://localhost:3306/student_systemdb
spring.datasource.username=root
spring.datasource.password=Divy@@1560

# format and highlight config
spring.jpa.hibernate.ddl-auto=create
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.use_sql_comments=true
spring.jpa.properties.hibernate.format_sql= false
spring.jpa.properties.hibernate.highlight_sql=true

#path setup
server.servlet.context-path=/manage/api

# to exclude the db config 
#spring.autoconfigure.exclude= org.springframework.boot.autoconfigure.jdbc.DataSourceAutoConfiguration

# disable started security for now
spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.security.servlet.SecurityAutoConfiguration

# Debug log
logging.level.org.springframework.security=DEBUG